import type { ReactNode } from "react";
export function InfoFeatureGrid({ items }: { items: { icon: ReactNode; title: string; description: string; accent: string }[] }) { return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map((item) => <article className="info-feature rounded-2xl border border-[#29435e] bg-[#0b1a2b]/70 p-5" key={item.title}><div className={`flex size-11 items-center justify-center rounded-xl ${item.accent}`}>{item.icon}</div><h3 className="mt-5 text-base font-semibold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></article>)}</div>; }
