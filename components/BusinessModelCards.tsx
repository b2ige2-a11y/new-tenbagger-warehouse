import type { BusinessSegment } from "@/data/types";
export function BusinessModelCards({ items }: { items: BusinessSegment[] }) { return <div className="grid gap-3 md:grid-cols-3">{items.map((item, index) => <article className="rounded-xl border border-[#29435e] bg-[#091827]/70 p-5" key={item.title}><span className="font-mono text-xs text-cyan-300">0{index + 1}</span><h3 className="mt-3 text-base font-semibold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p>{item.note && <p className="mt-4 text-xs text-sky-200">{item.note}</p>}</article>)}</div>; }
