export function SupplyChainDiagram({ items }: { items: { label: string; description: string }[] }) { return <div className="supply-chain" aria-label="공급망 흐름도">{items.map((item, index) => <div className="supply-chain__item" key={item.label}><div className="supply-chain__node"><span className="supply-chain__index">0{index + 1}</span><p className="supply-chain__label">{item.label}</p><p className="supply-chain__description">{item.description}</p></div>{index < items.length - 1 && <div className="supply-chain__arrow" aria-hidden="true"><span className="md:hidden">↓</span><span className="hidden md:inline">→</span></div>}</div>)}</div>; }
