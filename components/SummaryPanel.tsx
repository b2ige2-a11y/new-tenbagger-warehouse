export function SummaryPanel({ facts }: { facts: { label: string; value: string }[] }) { return <section className="summary-panel"><p className="eyebrow">한눈에 보기</p><div className="summary-panel__grid">{facts.map((fact) => <div className="summary-panel__item" key={fact.label}><p className="summary-panel__label">{fact.label}</p><p className="summary-panel__value">{fact.value}</p></div>)}</div></section>; }
