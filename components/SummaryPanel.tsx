export function SummaryPanel({ facts }: { facts: { label: string; value: string }[] }) { return <section className="rounded-2xl border border-cyan-300/25 bg-cyan-300/[.06] p-5 sm:p-6"><p className="eyebrow">이 페이지에서 바로 알 수 있는 것</p><div className="mt-4 grid gap-4 sm:grid-cols-3">{facts.map((fact) => <div key={fact.label}><p className="text-xs font-semibold text-cyan-200">{fact.label}</p><p className="mt-1.5 text-sm leading-6 text-slate-200">{fact.value}</p></div>)}</div></section>; }
