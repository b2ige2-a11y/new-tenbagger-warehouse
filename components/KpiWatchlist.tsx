import type { KpiItem } from "@/data/types";
import { formatDate } from "@/lib/utils";

function normalizeKpi(item: KpiItem) { return typeof item === "string" ? { label: item, status: "정기 점검 필요", description: "기업의 사업 흐름과 수요 변화를 함께 확인하는 관찰 지표입니다.", watchPoint: "다음 회사 공시와 실적 자료에서 변화 여부 확인", importance: "medium" as const } : { importance: "medium" as const, status: "정기 점검 필요", watchPoint: "다음 공식 자료에서 변화 여부 확인", ...item }; }

export function KpiWatchlist({ items }: { items: KpiItem[] }) { return <div className="grid gap-3">{items.map((item) => { const kpi = normalizeKpi(item); const tone = kpi.importance === "high" ? "border-cyan-300/35 bg-cyan-300/[.06]" : "border-[#29435e] bg-[#0b1a2b]/60"; return <article className={`rounded-xl border p-4 sm:p-5 ${tone}`} key={kpi.label}><div className="flex flex-wrap items-start justify-between gap-3"><h3 className="text-base font-semibold text-slate-100">{kpi.label}</h3><span className="rounded-full border border-cyan-300/25 bg-cyan-300/10 px-2.5 py-1 text-xs font-semibold text-cyan-100">{kpi.status}</span></div><p className="mt-3 text-sm leading-7 text-slate-400">{kpi.description}</p><div className="mt-4 border-t border-[#29435e]/70 pt-3"><p className="text-xs font-semibold text-slate-300">다음 체크 포인트</p><p className="mt-1 text-sm leading-6 text-slate-400">{kpi.watchPoint}</p>{kpi.lastChecked && <p className="mt-3 text-xs text-slate-500">마지막 점검일 · {formatDate(kpi.lastChecked)}</p>}</div></article>; })}</div>; }
