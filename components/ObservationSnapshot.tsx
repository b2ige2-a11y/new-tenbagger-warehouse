export function ObservationSnapshot({ items, lastUpdated }: { items: { title: string; status: string; detail: string }[]; lastUpdated: string }) { return <section className="observation-snapshot rounded-2xl p-5 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">현재 관찰 포인트</p><h2 className="mt-2 text-xl font-semibold">지금 이 기업을 볼 때 확인할 것</h2></div><p className="text-xs text-slate-500">실시간 데이터가 아닌 아카이브 점검 메모 · {lastUpdated}</p></div><div className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-4">{items.map((item) => <article className="rounded-xl border border-[#29435e] bg-[#091827]/60 p-4" key={item.title}><span className="inline-flex rounded-full bg-cyan-300/10 px-2 py-1 text-xs font-semibold text-cyan-100">{item.status}</span><h3 className="mt-3 text-sm font-semibold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{item.detail}</p></article>)}</div></section>; }
