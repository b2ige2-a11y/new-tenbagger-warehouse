import Link from "next/link";
import type { Stock } from "@/data/types";
import { TagBadge } from "./TagBadge";
export function StockCard({ stock }: { stock: Stock }) { return <Link href={`/stocks/${stock.slug}`} className="panel group rounded-xl p-5 transition hover:-translate-y-0.5 hover:border-cyan-300/50"><div className="flex items-start justify-between gap-3"><div><p className="text-lg font-semibold">{stock.name}</p><p className="mt-1 text-xs text-slate-500">{stock.exchange} · {stock.sector}</p></div><span className="rounded bg-cyan-300/10 px-2 py-1 font-mono text-xs font-bold text-cyan-200">{stock.ticker}</span></div><p className="mt-5 min-h-10 text-sm leading-5 text-slate-300">{stock.summary}</p><div className="mt-5 flex flex-wrap gap-1.5">{stock.themes.slice(0, 2).map((theme) => <TagBadge key={theme} label={theme.replaceAll("-", " ")} />)}</div></Link>; }
