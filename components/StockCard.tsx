import Link from "next/link";
import type { Stock } from "@/data/types";

const themeNames: Record<string, string> = { "ai-infrastructure": "AI 인프라", semiconductor: "반도체", "hbm-memory": "HBM", space: "우주", "power-grid": "전력" };

export function StockCard({ stock }: { stock: Stock }) { const keywordFact = stock.quickFacts?.find((fact) => fact.label.includes("키워드")); const keywords = keywordFact ? keywordFact.value.split(" · ").slice(0, 3) : stock.themes.map((theme) => themeNames[theme] ?? theme); const thesis = stock.thesis?.statement ?? stock.summary; return <Link href={`/stocks/${stock.slug}`} className="friendly-stock-card"><div className="flex items-start justify-between gap-3"><div><p className="friendly-stock-card__name">{stock.name}</p><p className="friendly-stock-card__meta">{stock.exchange} · {stock.sector}</p></div><span className="friendly-stock-card__ticker">{stock.ticker}</span></div>{stock.thesis && <p className="friendly-stock-card__status">Thesis {stock.thesis.status}</p>}<p className="friendly-stock-card__summary">{thesis}</p><div className="friendly-stock-card__keywords">{keywords.map((keyword) => <span key={keyword}>{keyword}</span>)}</div><p className="friendly-stock-card__link">핵심 관점 보기 →</p></Link>; }
